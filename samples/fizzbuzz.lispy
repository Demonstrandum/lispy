(define (fizz x)
  (if (divisible x 3)
    (if (divisible x 5)
      "FizzBuzz"
      "Fizz")
    (if (divisible x 5)
      "Buzz"
      (string x))))

(let (a (range 1 20)))

(puts "\nPlain:" a)
(puts "\nFizz'd:" (map fizz a))

(puts)
